# Vanilla Reply Homebrew Tap

Private Homebrew tap for Vanilla Reply tools.

## Install

```sh
brew tap vanilla-reply/tap git@github.com:vanilla-reply/homebrew-tap.git
brew install vanilla-reply/tap/neurons-opencode-bridge
```

## Update

```sh
brew update
brew upgrade neurons-opencode-bridge
```
